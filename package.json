{
	"name": "mern-notes-app",
	"version": "1.0.0",
	"description": "",
	"main": "app.js",
	"scripts": {
		"client": " cd front-end && npm start",
		"server": "nodemon back-end/app.js",
		"build": "cd front-end && npm run build",
		"start": "npm run build && node back-end/app.js",
		"dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Facundo Benitez",
	"license": "ISC",
	"dependencies": {
		"dotenv": "^10.0.0",
		"express": "^4.17.1",
		"mongoose": "^6.0.0"
	},
	"devDependencies": {
		"concurrently": "^6.2.1",
		"eslint": "^7.32.0",
		"nodemon": "^2.0.12"
	}
}